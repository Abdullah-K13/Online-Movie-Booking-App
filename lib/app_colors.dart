import 'dart:ui';

import 'package:flutter/material.dart';
class app_colors extends StatelessWidget{

final  Color maincolor = Color(0xFFFF0000);

final Color primarycolor = Color(0xFF3D0000);

final Color Text = Color(0xFFFBFADA);


  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}